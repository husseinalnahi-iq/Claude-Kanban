{
  "name": "claude-kanban",
  "version": "0.1.0",
  "private": true,
  "description": "A board on your own computer where Claude plans, codes and reviews your tasks — in parallel, with approvals, and an honest record of what it cost.",
  "license": "MIT",
  "author": "Hussein Alnahi",
  "repository": {
    "type": "git",
    "url": "https://github.com/husseinalnahi-iq/Claude-Kanban.git"
  },
  "homepage": "https://github.com/husseinalnahi-iq/Claude-Kanban#readme",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build -w web",
    "start": "npm run build && npm run start -w server",
    "test": "npm test -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w web"
  },
  "engines": {
    "node": ">=24"
  }
}
